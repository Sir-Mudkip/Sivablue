Metasploit is now aliased. To run, start a new terminal session with:"
`podman-msf`

To run a persistent Metasploit session with a db, run:"
`podman-msf CUSTOMER_NAME`

To reconnect to that msf session, run:
`podman-msf-connect CUSTOMER_NAME`
