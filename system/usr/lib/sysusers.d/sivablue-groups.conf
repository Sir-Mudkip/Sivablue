# Groups for packaged daemons whose RPM scriptlets write straight to
# /etc/group. On a bootc image /etc is machine-local, so a group that exists
# only there is not reproducible across deployments; declaring it here lets
# systemd-sysusers own it and satisfies bootc container lint's sysusers check.
g docker -
g libvirt -
