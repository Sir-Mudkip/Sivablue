# Do not pull and stage an image update while running on battery. systemd treats
# this condition as met when the machine has no AC connector at all, so a desktop
# is unaffected; only a laptop on battery skips the scheduled run.
# uupd-on-ac.service catches the skipped run up when power is reconnected.
[Unit]
ConditionACPower=true
