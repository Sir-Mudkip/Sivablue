name: Validate Justfiles
permissions:
  contents: read

on:
  pull_request:
    paths:
      - "system/usr/share/sivablue/just/**"
      - "Justfile"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Install just
        uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3 # v4

      - name: Validate Justfiles
        shell: bash
        run: |
          echo "Validating justfiles in system/usr/share/sivablue/just/ directory..."

          set -xeuo pipefail

          # Validate root Justfile
          echo "::group:: Validating Justfile"
          just --unstable --fmt --check -f Justfile
          echo "::endgroup::"

          # Validate the ujust modules
          find "system/usr/share/sivablue/just" -iname '*.just' -print0 | \
            while IFS= read -r -d '' justfile ; do \
              echo "::group:: ===$(basename "$justfile")==="
              just --unstable --fmt --check -f "$justfile"
              echo "::endgroup::"
            done

          echo "All justfiles are valid"
